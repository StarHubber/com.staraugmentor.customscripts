using StarCooperation;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

namespace STAR.DaimlerTrucks
{
    public class SetUpBackSubBtn : MonoBehaviour
    {
        public Button backButton;

        // Start is called before the first frame update
        void Start()
        {
            //backButton.onClick.AddListener(SceneManager.instance.GetComponent<PanelAndHotspotHandler>().GotoPanel());
            //backButton.onClick.AddListener(SceneManager.instance.GetComponent<PanelAndHotspotHandler>().ShowTooltips());
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
